# Production dependencies for Hybrid Cloud Controller
# Compile with: uv pip-compile requirements.piptools -o requirements.txt

flask>=3.0.0
sqlalchemy>=2.0.0
alembic>=1.13.0
boto3>=1.34.0
bcrypt>=4.1.0
cryptography>=42.0.0
hypothesis>=6.98.0
python-terraform>=0.10.1
docker>=7.0.0
